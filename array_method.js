let num = [1, 2, 3, 46, 4]
let b =num.toString() //b is now a string

let c = num.join(" & ") // joing & in an array

num.pop()
console.log(num) // popped element

let r = num.push(21)// pushed element
 let sft = num.shift(19) //remove the element in start of the array

 let unsft = num.unshift(94)// add the element in start of an array
