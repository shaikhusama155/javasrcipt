function onePlusAvg(x, y) {
  return 1 + (x + y) / 2
}
let x = 1;
let y = 2;
let a = 1;
let b = 2;
let c = 3;
console.log("one plus Average of a and b is ", onePlusAvg(x, y))
console.log("one plus Average of b and c is ", onePlusAvg(a, b))
console.log("one plus Average of a and c is ", onePlusAvg(c, b))